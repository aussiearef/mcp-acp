# Install dependencies when the workspace starts
install:
	pip install --quiet fastmcp mcp

# Optional: run something to verify install
test:
	python -c "import fastmcp, mcp; print('✅ fastmcp and mcp installed')"
